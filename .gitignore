# Local environment — never commit (see .env.example for the template)
.env

# Owner-specific content artifacts
content-review/pending/
content-review/approved/
content-review/rejected/

# Local data + caches
node_modules/
data/
.cache/
*.log
tsconfig.web.tsbuildinfo
fixes.txt
